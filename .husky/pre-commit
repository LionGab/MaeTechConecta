#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Pre-commit hook automático - SKIP PERMISSIONS
# Executa validações automaticamente sem pedir confirmação

echo "🔍 Pre-commit: Executando validações automáticas..."

# Lint fix automático
npm run lint:fix --quiet || true

# Format automático
npm run format --quiet || true

# Validação (sem bloquear se falhar)
npm run lint --quiet || echo "⚠️  Lint warnings (continuando...)"
npm run type-check --quiet || echo "⚠️  Type check warnings (continuando...)"

echo "✅ Pre-commit concluído!"
