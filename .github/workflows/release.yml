name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Create Release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          body: |
            ## 🎉 Nova Versão!
            
            ### Mudanças
            - Implementadas funcionalidades X
            - Corrigido bug Y
            - Melhorias de performance
            
            ### Download
            - iOS: [Em breve]
            - Android: [Em breve]
          draft: false
          prerelease: false

